import AsyncStorage from "@react-native-async-storage/async-storage";
import { router, Stack, Tabs } from "expo-router";
import { Button, Text, View } from "react-native";

const Exchange = () => {
    return (
        <View>
            <Text>Estou na Exchange</Text>
        </View>
    );
};
export default Exchange;
