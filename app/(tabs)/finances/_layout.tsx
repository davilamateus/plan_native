import AsyncStorage from "@react-native-async-storage/async-storage";
import { router, Stack, Tabs } from "expo-router";
import { Button, Text, View } from "react-native";

const Finances = () => {
    return (
        <View>
            <Text>Estou na Finances</Text>
        </View>
    );
};
export default Finances;
