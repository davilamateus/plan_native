import AsyncStorage from "@react-native-async-storage/async-storage";
import { router, Stack, Tabs } from "expo-router";
import { Button, Text, View } from "react-native";

const Todolist = () => {
    return (
        <View>
            <Text>Estou na Todolist</Text>
        </View>
    );
};
export default Todolist;
